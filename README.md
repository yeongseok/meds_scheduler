
  # Meds_scheduler

  This is a code bundle for Meds_scheduler. The original project is available at https://www.figma.com/design/EwllSwxsTYt0eqEFbbmDIV/Meds_scheduler.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  